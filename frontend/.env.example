# Backend server port
# Frontend will use window.location.hostname with this port to construct the backend URL.
# This allows automatic routing from any address (localhost, IPv4, Tailscale, etc.)
VITE_BACKEND_PORT=4200
